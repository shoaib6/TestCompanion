<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestCompanion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/abeeze_regular</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_purple</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.TestCompanion" parent="Base.Theme.TestCompanion" />

    <style name="background_theme_color">
        <item name="android:background">@color/app_background_color</item>
    </style>

    <style name="toolbar_text">
        <item name="android:fontFamily">@font/abeeze_regular</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ButtonNoShadow" parent="Widget.AppCompat.Button">
        <item name="android:stateListAnimator">@null</item>
    </style>

</resources>